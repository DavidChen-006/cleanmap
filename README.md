# cleanmap

CLI tool for cleaning images — strips clutter (cars, people, trees) and returns the main subject.

## Usage

```
cleanmap input.png --output cleaned.png
```

## Install

```
pip install -e .
```

## Status

Early scaffold. See `pyproject.toml` for dependencies.
